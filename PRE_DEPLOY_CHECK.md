# Преддеплойная проверка проекта

## ✅ Выполненные исправления

### 1. Код и импорты
- ✅ Удалены неиспользуемые импорты (`F`, `Window`, `RowNumber`) из `api/views.py`
- ✅ Убраны дублирующиеся импорты в `screener/views.py`
- ✅ Исправлена ошибка: удалена ссылка на несуществующее поле `vdelta_1m` в `api/views.py`
- ✅ Упрощены комментарии в коде

### 2. Конфигурация Gunicorn
- ✅ Убраны лишние комментарии о диагностике
- ✅ Оставлены только необходимые настройки
- ✅ Оптимизирована конфигурация для production

### 3. Оптимизация запросов
- ✅ Запросы оптимизированы для работы с большими таблицами
- ✅ Используется ограничение по времени (6 часов) для уменьшения нагрузки
- ✅ Применены индексы для ускорения запросов

### 4. Безопасность
- ✅ Используются переменные окружения для секретных данных
- ✅ CSRF защита включена
- ✅ XFrameOptions включен
- ✅ Валидация паролей настроена

## 📋 Проверка перед деплоем

### Обязательные проверки

#### 1. Переменные окружения (.env)
```bash
# Проверить наличие всех необходимых переменных
cat .env | grep -v PASSWORD

# Обязательные переменные:
# - DJANGO_SECRET_KEY (должен быть уникальным и секретным!)
# - DJANGO_DEBUG=False (для production)
# - ALLOWED_HOSTS (должен содержать домен/IP сервера)
# - DB_NAME, DB_USER, DB_PASSWORD, DB_HOST, DB_PORT
# - TELEGRAM_BOT_TOKEN (если используется)
```

#### 2. База данных
```bash
# Проверить миграции
python manage.py showmigrations

# Применить миграции если нужно
python manage.py migrate

# Проверить индексы
sudo -u postgres psql -d cryptoscreener -c "\d screener_screenersnapshot"
```

#### 3. Статические файлы
```bash
# Собрать статические файлы
python manage.py collectstatic --noinput

# Проверить права доступа
ls -la staticfiles/
```

#### 4. Тестирование
```bash
# Проверить Django
python manage.py check

# Проверить загрузку Django
python test_django.py

# Проверить работу команды очистки
python manage.py cleanup_old_snapshots --dry-run
```

#### 5. Конфигурация сервисов
```bash
# Проверить systemd сервисы
sudo systemctl status noetdat.service
sudo systemctl status noetdat-bot.service

# Проверить Nginx конфигурацию
sudo nginx -t

# Проверить cron jobs
crontab -l
```

## 🔒 Безопасность

### Production настройки

1. **DEBUG должен быть False:**
   ```python
   DEBUG = os.getenv("DJANGO_DEBUG", "False") == "True"
   # В .env: DJANGO_DEBUG=False
   ```

2. **SECRET_KEY должен быть уникальным:**
   ```bash
   # Сгенерировать новый ключ
   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
   ```

3. **ALLOWED_HOSTS должен содержать домен:**
   ```python
   ALLOWED_HOSTS = ["your-domain.com", "your-server-ip"]
   ```

4. **HTTPS (рекомендуется):**
   - Настроить SSL через Let's Encrypt
   - Обновить Nginx конфигурацию для HTTPS

## 📊 Производительность

### Оптимизации

1. **База данных:**
   - Индексы созданы на `(symbol, -ts)`
   - Автоматическая очистка старых данных (24 часа)
   - Ограничение запросов последними 6 часами

2. **Gunicorn:**
   - Оптимальное количество воркеров (2-4)
   - Увеличенный timeout (300 секунд)
   - Логирование настроено

3. **Статические файлы:**
   - Собраны через `collectstatic`
   - Обслуживаются через Nginx
   - Кеширование настроено

## 🚀 Деплой чеклист

### Перед деплоем

- [ ] Все миграции применены
- [ ] Статические файлы собраны
- [ ] Переменные окружения настроены
- [ ] DEBUG=False в production
- [ ] SECRET_KEY уникальный и секретный
- [ ] ALLOWED_HOSTS содержит домен
- [ ] База данных создана и настроена
- [ ] Индексы созданы
- [ ] Nginx конфигурация проверена
- [ ] Gunicorn сервис настроен
- [ ] Cron jobs настроены (очистка данных)
- [ ] Telegram bot настроен (если используется)
- [ ] Скрипты ingest настроены (если используются)

### После деплоя

- [ ] Проверить доступность сайта
- [ ] Проверить работу API
- [ ] Проверить логи Gunicorn
- [ ] Проверить логи Nginx
- [ ] Проверить работу cron jobs
- [ ] Проверить использование ресурсов (CPU, память)
- [ ] Проверить размер базы данных
- [ ] Настроить мониторинг (опционально)

## 📝 Известные ограничения

1. **Ограничение данных:** Запросы работают только с данными за последние 6 часов
2. **Автоочистка:** Данные старше 24 часов автоматически удаляются
3. **Воркеры Gunicorn:** Максимум 4 воркера для стабильности

## 🔧 Устранение проблем

### Если сайт не загружается

1. Проверить статус Gunicorn: `sudo systemctl status noetdat.service`
2. Проверить логи: `tail -50 logs/gunicorn_error.log`
3. Проверить Nginx: `sudo nginx -t`
4. Проверить базу данных: `sudo systemctl status postgresql`

### Если воркеры падают

1. Проверить использование памяти: `free -h`
2. Проверить использование CPU: `top`
3. Уменьшить количество воркеров в `gunicorn_config.py`
4. Проверить размер базы данных

### Если данные не обновляются

1. Проверить работу скриптов ingest
2. Проверить подключение к Binance API
3. Проверить логи скриптов

## 📚 Документация

- `README.md` - Основная документация
- `DEPLOY.md` - Инструкции по деплою
- `CLEANUP.md` - Автоматическая очистка данных
- `FIX_504.md` - Решение проблем с таймаутами
- `CHECK_CPU.md` - Диагностика загрузки CPU

## ✅ Итог

Проект готов к деплою. Все основные проблемы исправлены, код оптимизирован, безопасность настроена.

